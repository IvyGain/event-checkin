# デプロイガイド

このガイドでは、イベントチェックインシステムをVercelにデプロイする手順を説明します。

---

## 前提条件

- GitHubアカウント
- Vercelアカウント（無料）
- Resendアカウント（メール送信用、無料枠あり）

---

## ステップ1: Resend APIキーの取得

### 1.1 Resendアカウント作成
1. https://resend.com にアクセス
2. 「Sign Up」をクリック
3. メールアドレスで登録

### 1.2 APIキー作成
1. ダッシュボードで「API Keys」をクリック
2. 「Create API Key」をクリック
3. 名前を入力（例: event-checkin-production）
4. 「Create」をクリック
5. **APIキーをコピーして安全に保存**（`re_`で始まる文字列）

### 1.3 送信元メールアドレス設定
1. 「Domains」タブをクリック
2. 自分のドメインを追加（または無料のonboarding@resend.devを使用）
3. DNSレコードを設定（独自ドメイン使用の場合）

**注意**: 無料プランでは `onboarding@resend.dev` から送信可能ですが、本番環境では独自ドメインの設定を推奨します。

---

## ステップ2: GitHubリポジトリ作成

### 2.1 GitHubにプッシュ
```bash
cd event-checkin

# まだGitHubリモートが設定されていない場合
gh repo create event-checkin --public --source=. --remote=origin --push

# または既存のリモートにプッシュ
git push origin main
```

---

## ステップ3: Vercelデプロイ

### 3.1 Vercelアカウント作成
1. https://vercel.com にアクセス
2. 「Sign Up」をクリック
3. GitHubアカウントで登録

### 3.2 プロジェクトインポート
1. Vercelダッシュボードで「Add New Project」をクリック
2. 「Import Git Repository」を選択
3. `event-checkin` リポジトリを選択
4. 「Import」をクリック

### 3.3 環境変数設定
「Environment Variables」セクションで以下を設定:

```
DATABASE_URL = postgresql://...  (後で設定)
RESEND_API_KEY = re_xxxxx  (ステップ1で取得したキー)
EMAIL_FROM = noreply@yourdomain.com  (または onboarding@resend.dev)
NEXT_PUBLIC_APP_URL = https://your-app.vercel.app  (デプロイ後に更新)
```

**重要**: まず `DATABASE_URL` は空欄でデプロイし、後でPostgreSQLを設定します。

### 3.4 デプロイ実行
1. 「Deploy」をクリック
2. ビルドが完了するまで待機（約2-3分）
3. デプロイ完了後、URLが表示されます（例: `https://event-checkin-xxx.vercel.app`）

---

## ステップ4: PostgreSQLデータベース設定

### 4.1 Vercel Postgres作成
1. Vercelプロジェクトダッシュボードで「Storage」タブをクリック
2. 「Create Database」をクリック
3. 「Postgres」を選択
4. データベース名を入力（例: `event-checkin-db`）
5. リージョンを選択（日本の場合: `Tokyo, Japan`）
6. 「Create」をクリック

### 4.2 環境変数の自動設定
Vercel Postgresを作成すると、`DATABASE_URL` などの環境変数が自動的に設定されます。

### 4.3 Prismaマイグレーション実行
Vercelダッシュボードで:
1. 「Settings」→「Environment Variables」で `DATABASE_URL` が設定されていることを確認
2. 「Deployments」タブ→最新デプロイの「...」→「Redeploy」
3. 「Use existing Build Cache」のチェックを外す
4. 「Redeploy」をクリック

または、ローカルから実行:
```bash
# 本番データベースURLを一時的に設定
export DATABASE_URL="postgresql://..."  # Vercelからコピー

# マイグレーション実行
npx prisma migrate deploy

# Prisma Clientを生成
npx prisma generate
```

---

## ステップ5: 環境変数の最終確認と更新

### 5.1 APP URL更新
1. Vercelダッシュボード→「Settings」→「Environment Variables」
2. `NEXT_PUBLIC_APP_URL` を更新
   - 値: `https://your-actual-domain.vercel.app`
3. 「Save」をクリック
4. 再デプロイ（「Deployments」→「Redeploy」）

### 5.2 環境変数一覧の最終確認
```
DATABASE_URL = postgresql://...  (Vercel Postgres)
RESEND_API_KEY = re_xxxxx
EMAIL_FROM = noreply@yourdomain.com
NEXT_PUBLIC_APP_URL = https://your-app.vercel.app
```

---

## ステップ6: 動作確認

### 6.1 アプリケーションアクセス
1. デプロイされたURLにアクセス
2. ホームページが表示されることを確認

### 6.2 機能テスト
1. **管理画面**: `/admin` にアクセス
2. **イベント作成**: 新規イベントを作成
3. **参加者登録**: テスト参加者を追加
4. **QRコード生成**: QRコードが表示されることを確認
5. **メール送信**: テストメールを送信（自分のメールアドレスで）
6. **チェックイン**: `/checkin` でQRコードをスキャン

---

## トラブルシューティング

### データベースエラー
**エラー**: `PrismaClientInitializationError`

**解決策**:
1. `DATABASE_URL` が正しく設定されているか確認
2. Prismaマイグレーションが実行されているか確認
   ```bash
   npx prisma migrate deploy
   ```

### メール送信エラー
**エラー**: `Email service not configured`

**解決策**:
1. `RESEND_API_KEY` が設定されているか確認
2. Resend APIキーが有効か確認
3. 送信元メールアドレスがResendで認証されているか確認

### QRコード表示エラー
**エラー**: QRコードのURLが間違っている

**解決策**:
1. `NEXT_PUBLIC_APP_URL` が正しいURLに設定されているか確認
2. 再デプロイ

### ビルドエラー
**エラー**: TypeScript型エラー

**解決策**:
```bash
# ローカルで型チェック
npm run build

# エラーを修正してコミット
git add .
git commit -m "fix: Fix TypeScript errors"
git push
```

---

## カスタムドメイン設定（オプション）

### 1. ドメイン追加
1. Vercelダッシュボード→「Settings」→「Domains」
2. 「Add」をクリック
3. ドメイン名を入力（例: `checkin.yourdomain.com`）
4. 「Add」をクリック

### 2. DNS設定
Vercelが提示するDNSレコードをドメインプロバイダーで設定:
```
Type: CNAME
Name: checkin  (またはサブドメイン)
Value: cname.vercel-dns.com
```

### 3. SSL証明書
Vercelが自動的にSSL証明書を発行（Let's Encrypt）

### 4. 環境変数更新
```
NEXT_PUBLIC_APP_URL = https://checkin.yourdomain.com
EMAIL_FROM = noreply@yourdomain.com
```

---

## セキュリティ設定

### 1. 管理画面の保護
現在、管理画面にアクセス制限がありません。以下のいずれかを実装することを推奨:

#### オプション1: Vercel Authentication
```bash
# Vercel CLI インストール
npm i -g vercel

# パスワード保護設定
vercel env add PASSWORD
```

#### オプション2: NextAuth.js実装
将来の機能として、NextAuth.jsによる認証システムの追加を推奨。

### 2. 環境変数の保護
- 絶対に `.env` ファイルをGitにコミットしない
- `.gitignore` に `.env` が含まれていることを確認

### 3. レート制限
本番環境では、API エンドポイントにレート制限を追加することを推奨。

---

## モニタリング

### Vercel Analytics
1. Vercelダッシュボード→「Analytics」タブ
2. ページビュー、パフォーマンス指標を確認

### Logs
1. Vercelダッシュボード→「Logs」タブ
2. エラーログ、アクセスログを確認

### エラー監視（推奨）
Sentryなどのエラー監視サービスの導入を推奨:
```bash
npm install @sentry/nextjs
npx @sentry/wizard -i nextjs
```

---

## バックアップ

### データベースバックアップ
Vercel Postgresは自動バックアップを提供していますが、手動バックアップも推奨:

```bash
# データベースをエクスポート
pg_dump $DATABASE_URL > backup.sql

# リストア
psql $DATABASE_URL < backup.sql
```

---

## スケーリング

### Vercel Pro プラン
無料プランの制限:
- 100GB 帯域幅/月
- 6,000分ビルド時間/月

大規模イベントの場合、Pro プランへのアップグレードを検討。

### データベーススケーリング
参加者数が1,000人を超える場合:
- Vercel Postgres の上位プランを検討
- または、Supabase、PlanetScale などの専用データベースサービスを使用

---

## アップデート

### コード更新
```bash
# ローカルで変更
git add .
git commit -m "feat: Add new feature"
git push origin main

# Vercelが自動的に再デプロイ
```

### 環境変数更新
1. Vercelダッシュボード→「Settings」→「Environment Variables」
2. 変更したい変数を編集
3. 「Save」をクリック
4. 再デプロイが必要な場合は「Redeploy」

---

## サポート

### 公式ドキュメント
- Vercel: https://vercel.com/docs
- Prisma: https://www.prisma.io/docs
- Resend: https://resend.com/docs
- Next.js: https://nextjs.org/docs

### コミュニティ
- Vercel Discord: https://vercel.com/discord
- Prisma Discord: https://pris.ly/discord

---

デプロイ完了！お疲れ様でした 🎉
